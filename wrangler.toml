name = "trump-tweet-monitor"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# KV命名空间绑定（用于存储已推送的推文ID）
[[kv_namespaces]]
binding = "TRUMP_KV"
id = "82bfdc175ad84feb8073147daadcf0f0"

# Cron触发器：每分钟执行一次
[triggers]
crons = ["* * * * *"]
